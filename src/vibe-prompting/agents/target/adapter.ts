/**
 * Owns the provider-neutral contract for agents under evaluation.
 * Messages and requested and actual model IDs are required.
 * Usage, latency, trace references, and provider details are optional metadata.
 * Runtime failures reject the adapter call.
 */

export {};
