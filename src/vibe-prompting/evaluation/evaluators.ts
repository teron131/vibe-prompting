/**
 * Owns reusable scoring behavior independently of workflow orchestration.
 * Aggregation excludes unknown and inapplicable scores.
 * It validates partial and applicability policy.
 * Failed and unresolved hard gates remain separate from the weighted average.
 */

export {};
