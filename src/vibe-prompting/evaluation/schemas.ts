/**
 * Owns the canonical persisted configuration and evaluation evidence shapes.
 * The schemas remain portable across execution frameworks.
 * LangGraph state, provider payloads, and Langfuse records stay at their own boundaries.
 */

export {};
