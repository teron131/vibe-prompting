/** Publishes the application database client and explicit administrative setup operation. */

export { Database, type DatabaseClient } from "./client.ts";
export { setupDatabase } from "./setup.ts";
